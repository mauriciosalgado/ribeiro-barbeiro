[project]
name = "shop"
version = "0.1.0"
description = "The barber-shop booking website (Reflex)."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "reflex>=0.9.7",
]
