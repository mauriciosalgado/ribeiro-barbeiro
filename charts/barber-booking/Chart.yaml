apiVersion: v2
name: barber-booking
description: >-
  Online appointment booking for a barber shop — FastAPI backend + Reflex
  frontend, with an optional built-in Postgres. One install = one shop;
  install it again with a different release name/values for another shop.
type: application
version: 0.1.0
appVersion: "1.0.0"
